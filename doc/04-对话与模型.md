# AgentHub - 对话与模型

---

## 1. 业务概述

用户通过 AgentHub Web 界面与 Hermes Agent 实例进行对话。AgentHub 充当 WebSocket 代理，将用户消息转发到 Hermes 容器的 8642 端口，并将 Hermes 的流式响应回传给前端。

同时管理 API 密钥（用于连接模型提供商）和实例级别的模型配置。

---

## 2. 对话架构

```
┌─────────┐      WebSocket       ┌──────────┐    HTTP/WS Proxy    ┌────────────────┐
│ 前端    │ ◄──────────────────► │ AgentHub │ ◄──────────────────► │ Hermes 容器    │
│ Chat UI │ ws://host:8000/ws/  │ Backend  │ http://localhost:    │ :8642          │
│         │  chat/{instance_id}  │          │  {port}/v1/chat/    │                │
└─────────┘                      └──────────┘  completions         └────────────────┘
```

**流程**:

1. 前端建立 WebSocket 连接: `ws://host:8000/ws/chat/{instance_id}`
2. 用户发送消息 `{ type: "message", content: "你好" }`
3. 后端将消息转发到 Hermes 容器 HTTP 接口
4. Hermes 以 SSE 流式返回，后端实时推送到 WebSocket
5. 前端逐 token 渲染

---

## 3. WebSocket 协议

### 3.1 客户端 → 服务端

**发送消息**:

```json
{
  "type": "message",
  "content": "帮我写一个Python函数",
  "conversation_id": "uuid-xxx"
}
```

**中断生成**:

```json
{
  "type": "stop"
}
```

**心跳**:

```json
{
  "type": "ping"
}
```

### 3.2 服务端 → 客户端

**流式文本块**:

```json
{
  "type": "chunk",
  "content": "好的",
  "conversation_id": "uuid-xxx"
}
```

**生成完成**:

```json
{
  "type": "done",
  "conversation_id": "uuid-xxx"
}
```

**错误通知**:

```json
{
  "type": "error",
  "message": "Hermes 容器连接失败"
}
```

**心跳回应**:

```json
{
  "type": "pong"
}
```

---

## 4. 模型配置 API

### 4.1 GET /api/instances/{id}/model-config

获取实例的模型配置。

**成功响应**:

```json
{
  "code": 0,
  "data": {
    "id": 1,
    "instance_id": 1,
    "provider": "deepseek",
    "model_name": "deepseek-chat",
    "api_base_url": "https://api.deepseek.com",
    "temperature": 0.7,
    "max_tokens": 4096,
    "system_prompt": "你是一个有用的助手",
    "extra_params": {}
  }
}
```

### 4.2 PUT /api/instances/{id}/model-config

更新实例的模型配置。

**请求**:

```json
{
  "provider": "deepseek",
  "model_name": "deepseek-chat",
  "api_base_url": "https://api.deepseek.com",
  "temperature": 0.7,
  "max_tokens": 4096,
  "system_prompt": "你是一个有用的助手"
}
```

> 模型配置变更后，需要重启实例才能生效（env_config 中的 API Key 需匹配）。

---

## 5. API 密钥管理

### 5.1 密钥加密存储

所有 API 密钥使用 AES-256-GCM 加密后存储在数据库中。

```python
from cryptography.hazmat.primitives.ciphers.aead import AESGCM
import os, base64

class SecretEncryptor:
    def __init__(self, key: bytes):  # 32 bytes from env AES_SECRET_KEY
        self.aesgcm = AESGCM(key)

    def encrypt(self, plaintext: str) -> str:
        nonce = os.urandom(12)
        ct = self.aesgcm.encrypt(nonce, plaintext.encode(), None)
        return base64.b64encode(nonce + ct).decode()

    def decrypt(self, ciphertext: str) -> str:
        raw = base64.b64decode(ciphertext)
        nonce, ct = raw[:12], raw[12:]
        return self.aesgcm.decrypt(nonce, ct, None).decode()
```

### 5.2 GET /api/secrets

获取当前用户的 API 密钥列表（脱敏）。

**成功响应**:

```json
{
  "code": 0,
  "data": [
    {
      "id": 1,
      "name": "DeepSeek 主密钥",
      "provider": "deepseek",
      "key_masked": "sk-****abcd",
      "last_used_at": "2026-05-08T10:00:00",
      "created_at": "2026-05-01T08:00:00"
    }
  ]
}
```

### 5.3 POST /api/secrets

创建新密钥。

**请求**:

```json
{
  "name": "DeepSeek 主密钥",
  "provider": "deepseek",
  "api_key": "sk-abcdefg123456"
}
```

**成功响应**:

```json
{
  "code": 0,
  "data": {
    "id": 2,
    "name": "DeepSeek 主密钥",
    "provider": "deepseek",
    "key_masked": "sk-****3456"
  }
}
```

### 5.4 DELETE /api/secrets/{id}

删除密钥。

**前置条件**: 该密钥未被任何运行中的实例引用。

**失败**:

| code | message |
|------|---------|
| -1 | 该密钥正在被实例使用，无法删除 |

---

## 6. 前端页面设计

### 6.1 对话页 `/user/chat`（仅普通用户）

```
┌────────────────────────────────────────────────────────┐
│  [实例列表 ▼ 我的助手]                      [设置 ⚙]  │
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌────────────────────────────────────────────┐       │
│  │                                            │       │
│  │  用户: 帮我写一个Python排序函数             │       │
│  │                                            │       │
│  │  助手: 好的，这是一个冒泡排序的实现：        │       │
│  │  ```python                                 │       │
│  │  def bubble_sort(arr):                     │       │
│  │      n = len(arr)                          │       │
│  │      ...                                   │       │
│  │  ```                                       │       │
│  │                                            │       │
│  │  (流式输出中...)                           │       │
│  │                                            │       │
│  └────────────────────────────────────────────┘       │
│                                                        │
│  ┌──────────────────────────────────┐ [发送]  [停止]   │
│  │  输入消息...                      │                  │
│  └──────────────────────────────────┘                  │
└────────────────────────────────────────────────────────┘
```

**组件结构**:

```
views/user/chat/
├── index.vue              # 对话页主组件
├── MessageList.vue         # 消息列表（虚拟滚动）
├── MessageItem.vue         # 单条消息（支持 Markdown 渲染）
├── ChatInput.vue           # 输入框 + 发送/停止按钮
└── InstanceSelector.vue    # 实例选择下拉框
```

**关键实现**:

- Markdown 渲染: 使用 `markdown-it` + `highlight.js`
- 流式显示: WebSocket 收到 chunk 事件后追加到当前消息
- 自动滚动: 新消息到达时自动滚动到底部
- 代码复制: 代码块右上角一键复制按钮

### 6.2 密钥管理页 `/user/secrets`（仅普通用户）

```
┌─────────────────────────────────────────────────────────┐
│  API 密钥管理                              [+ 添加密钥] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 名称          │ 提供商   │ 密钥       │ 操作    │   │
│  ├───────────────┼─────────┼────────────┼─────────┤   │
│  │ DeepSeek 主   │ deepseek│ sk-****abcd│ [删除]  │   │
│  │ OpenAI 备用   │ openai  │ sk-****efgh│ [删除]  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**组件结构**:

```
views/user/secrets/
├── index.vue              # 密钥列表
└── CreateSecretDialog.vue  # 创建密钥弹窗
```

---

## 7. 后端关键文件

| 文件 | 职责 |
|------|------|
| `app/api/chat.py` | WebSocket 对话端点 |
| `app/api/secrets.py` | 密钥 CRUD |
| `app/api/model_config.py` | 模型配置 API |
| `app/services/chat_service.py` | 消息转发、流式代理 |
| `app/services/secret_service.py` | 加密/解密/脱敏 |
| `app/adapters/hermes_adapter.py` | 与 Hermes 容器 HTTP 接口通信 |
| `app/core/encryption.py` | AES-256-GCM 工具类 |
